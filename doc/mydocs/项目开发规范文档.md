# 租房避雷应用 - 项目开发规范文档

## 文档信息
- **文档类型**：给AI Agent开发使用
- **创建日期**：2026年2月24日
- **适用范围**：前端开发、后端开发、移动端开发

---

## 1. 概述

### 1.1 规范目的
- 统一开发风格
- 提高代码质量
- 降低维护成本
- 提升团队协作效率

### 1.2 规范范围
- 前端开发规范
- 后端开发规范
- 移动端开发规范
- 数据库开发规范
- Git工作流规范
- 代码审查规范

---

## 2. 前端开发规范

### 2.1 技术栈
- **框架**：React 18.x
- **语言**：TypeScript 5.x
- **状态管理**：Redux Toolkit
- **路由**：React Router 6.x
- **UI组件库**：Ant Design 5.x
- **HTTP客户端**：Axios
- **表单处理**：React Hook Form
- **样式方案**：CSS Modules + Tailwind CSS

### 2.2 项目结构

```
src/
├── assets/              # 静态资源
│   ├── images/         # 图片
│   ├── icons/          # 图标
│   └── fonts/          # 字体
├── components/         # 公共组件
│   ├── common/         # 通用组件
│   ├── layout/         # 布局组件
│   └── business/       # 业务组件
├── pages/              # 页面
│   ├── home/           # 首页
│   ├── community/      # 小区
│   ├── comment/        # 评论
│   ├── user/           # 用户
│   └── admin/          # 管理后台
├── hooks/              # 自定义Hooks
├── services/           # API服务
├── store/              # Redux状态管理
├── utils/              # 工具函数
├── types/              # TypeScript类型定义
├── styles/             # 全局样式
├── constants/          # 常量定义
├── config/             # 配置文件
└── App.tsx             # 应用入口
```

### 2.3 命名规范

#### 2.3.1 文件命名
- **组件文件**：PascalCase，如 `UserProfile.tsx`
- **工具文件**：camelCase，如 `formatDate.ts`
- **类型文件**：PascalCase，如 `UserTypes.ts`
- **常量文件**：PascalCase，如 `ApiConstants.ts`
- **样式文件**：camelCase，如 `userProfile.module.css`

#### 2.3.2 变量命名
- **变量**：camelCase，如 `userName`
- **常量**：UPPER_SNAKE_CASE，如 `MAX_FILE_SIZE`
- **布尔值**：is/has前缀，如 `isLoggedIn`、`hasPermission`
- **函数**：camelCase，如 `getUserInfo`
- **类**：PascalCase，如 `UserService`
- **接口**：PascalCase，I前缀，如 `IUser`
- **类型**：PascalCase，T前缀，如 `TUser`
- **枚举**：PascalCase，如 `UserRole`

#### 2.3.3 组件命名
- **组件名**：PascalCase，如 `UserProfile`
- **Props接口**：组件名+Props，如 `UserProfileProps`
- **自定义Hook**：use前缀，如 `useUserInfo`

### 2.4 代码规范

#### 2.4.1 组件定义

```typescript
import React from 'react';
import { FC } from 'react';

interface UserProfileProps {
  userId: number;
  userName: string;
  avatar?: string;
  onUpdate?: (userId: number) => void;
}

export const UserProfile: FC<UserProfileProps> = ({
  userId,
  userName,
  avatar,
  onUpdate
}) => {
  const handleUpdate = () => {
    onUpdate?.(userId);
  };

  return (
    <div className="user-profile">
      <img src={avatar} alt={userName} />
      <span>{userName}</span>
      <button onClick={handleUpdate}>更新</button>
    </div>
  );
};

export default UserProfile;
```

#### 2.4.2 Hooks使用

```typescript
import { useState, useEffect, useCallback } from 'react';

export const useUserInfo = (userId: number) => {
  const [userInfo, setUserInfo] = useState<IUser | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const fetchUserInfo = useCallback(async () => {
    setLoading(true);
    setError(null);
    try {
      const data = await UserService.getUserById(userId);
      setUserInfo(data);
    } catch (err) {
      setError(err.message);
    } finally {
      setLoading(false);
    }
  }, [userId]);

  useEffect(() => {
    fetchUserInfo();
  }, [fetchUserInfo]);

  return { userInfo, loading, error, refetch: fetchUserInfo };
};
```

#### 2.4.3 API服务

```typescript
import axios from 'axios';
import { API_BASE_URL } from '@/config/constants';

const apiClient = axios.create({
  baseURL: API_BASE_URL,
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json',
  },
});

apiClient.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

apiClient.interceptors.response.use(
  (response) => {
    return response.data;
  },
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);

export const UserService = {
  getUserById: (userId: number) => {
    return apiClient.get(`/users/${userId}`);
  },

  updateUser: (userId: number, data: Partial<IUser>) => {
    return apiClient.put(`/users/${userId}`, data);
  },

  deleteUser: (userId: number) => {
    return apiClient.delete(`/users/${userId}`);
  },
};
```

#### 2.4.4 Redux使用

```typescript
import { createSlice, createAsyncThunk, PayloadAction } from '@reduxjs/toolkit';

interface UserState {
  userInfo: IUser | null;
  loading: boolean;
  error: string | null;
}

const initialState: UserState = {
  userInfo: null,
  loading: false,
  error: null,
};

export const fetchUserInfo = createAsyncThunk(
  'user/fetchUserInfo',
  async (userId: number) => {
    const response = await UserService.getUserById(userId);
    return response.data;
  }
);

const userSlice = createSlice({
  name: 'user',
  initialState,
  reducers: {
    clearUserInfo: (state) => {
      state.userInfo = null;
    },
  },
  extraReducers: (builder) => {
    builder
      .addCase(fetchUserInfo.pending, (state) => {
        state.loading = true;
        state.error = null;
      })
      .addCase(fetchUserInfo.fulfilled, (state, action: PayloadAction<IUser>) => {
        state.loading = false;
        state.userInfo = action.payload;
      })
      .addCase(fetchUserInfo.rejected, (state, action) => {
        state.loading = false;
        state.error = action.error.message || '获取用户信息失败';
      });
  },
});

export const { clearUserInfo } = userSlice.actions;
export default userSlice.reducer;
```

### 2.5 样式规范

#### 2.5.1 CSS Modules

```css
.userProfile {
  display: flex;
  align-items: center;
  padding: 16px;
  border-radius: 8px;
  background-color: #ffffff;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.avatar {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  object-fit: cover;
}

.userName {
  margin-left: 12px;
  font-size: 16px;
  font-weight: 500;
  color: #333333;
}
```

#### 2.5.2 Tailwind CSS

```tsx
<div className="flex items-center p-4 rounded-lg bg-white shadow-md">
  <img
    src={avatar}
    alt={userName}
    className="w-12 h-12 rounded-full object-cover"
  />
  <span className="ml-3 text-base font-medium text-gray-800">
    {userName}
  </span>
</div>
```

### 2.6 性能优化

#### 2.6.1 使用React.memo

```typescript
import React, { memo } from 'react';

interface UserCardProps {
  user: IUser;
}

export const UserCard = memo<UserCardProps>(({ user }) => {
  return (
    <div className="user-card">
      <span>{user.name}</span>
    </div>
  );
});
```

#### 2.6.2 使用useMemo和useCallback

```typescript
import { useMemo, useCallback } from 'react';

export const UserList: FC<UserListProps> = ({ users }) => {
  const sortedUsers = useMemo(() => {
    return [...users].sort((a, b) => a.name.localeCompare(b.name));
  }, [users]);

  const handleUserClick = useCallback((userId: number) => {
    console.log('User clicked:', userId);
  }, []);

  return (
    <div>
      {sortedUsers.map((user) => (
        <UserCard key={user.id} user={user} onClick={handleUserClick} />
      ))}
    </div>
  );
};
```

---

## 3. 后端开发规范

### 3.1 技术栈
- **框架**：NestJS 10.x
- **语言**：TypeScript 5.x
- **数据库**：PostgreSQL 15.x
- **ORM**：Prisma 5.x
- **缓存**：Redis 7.x
- **验证**：class-validator
- **文档**：Swagger/OpenAPI
- **测试**：Jest

### 3.2 项目结构

```
src/
├── modules/            # 功能模块
│   ├── user/          # 用户模块
│   │   ├── dto/       # 数据传输对象
│   │   ├── entities/  # 实体定义
│   │   ├── guards/    # 守卫
│   │   ├── services/  # 服务
│   │   └── controllers/ # 控制器
│   ├── community/     # 小区模块
│   ├── comment/       # 评论模块
│   └── admin/         # 管理后台模块
├── common/            # 公共模块
│   ├── decorators/    # 装饰器
│   ├── filters/       # 异常过滤器
│   ├── interceptors/  # 拦截器
│   ├── pipes/         # 管道
│   └── guards/        # 守卫
├── config/            # 配置文件
├── database/          # 数据库相关
├── utils/             # 工具函数
├── types/             # 类型定义
└── main.ts            # 应用入口
```

### 3.3 命名规范

#### 3.3.1 文件命名
- **控制器**：*.controller.ts，如 `user.controller.ts`
- **服务**：*.service.ts，如 `user.service.ts`
- **DTO**：*.dto.ts，如 `create-user.dto.ts`
- **实体**：*.entity.ts，如 `user.entity.ts`
- **守卫**：*.guard.ts，如 `auth.guard.ts`
- **拦截器**：*.interceptor.ts，如 `logging.interceptor.ts`
- **管道**：*.pipe.ts，如 `validation.pipe.ts`

#### 3.3.2 变量命名
- **变量**：camelCase，如 `userName`
- **常量**：UPPER_SNAKE_CASE，如 `MAX_FILE_SIZE`
- **类**：PascalCase，如 `UserService`
- **接口**：PascalCase，I前缀，如 `IUser`
- **类型**：PascalCase，T前缀，如 `TUser`
- **枚举**：PascalCase，如 `UserRole`
- **方法**：camelCase，如 `getUserInfo`

### 3.4 代码规范

#### 3.4.1 控制器定义

```typescript
import { Controller, Get, Post, Put, Delete, Body, Param, Query, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiResponse } from '@nestjs/swagger';
import { JwtAuthGuard } from '@/common/guards/jwt-auth.guard';
import { UserService } from './user.service';
import { CreateUserDto } from './dto/create-user.dto';
import { UpdateUserDto } from './dto/update-user.dto';

@ApiTags('用户管理')
@Controller('users')
@UseGuards(JwtAuthGuard)
export class UserController {
  constructor(private readonly userService: UserService) {}

  @Get()
  @ApiOperation({ summary: '获取用户列表' })
  @ApiResponse({ status: 200, description: '成功' })
  async findAll(@Query() query: any) {
    return this.userService.findAll(query);
  }

  @Get(':id')
  @ApiOperation({ summary: '获取用户详情' })
  @ApiResponse({ status: 200, description: '成功' })
  async findOne(@Param('id') id: string) {
    return this.userService.findOne(+id);
  }

  @Post()
  @ApiOperation({ summary: '创建用户' })
  @ApiResponse({ status: 201, description: '创建成功' })
  async create(@Body() createUserDto: CreateUserDto) {
    return this.userService.create(createUserDto);
  }

  @Put(':id')
  @ApiOperation({ summary: '更新用户' })
  @ApiResponse({ status: 200, description: '更新成功' })
  async update(@Param('id') id: string, @Body() updateUserDto: UpdateUserDto) {
    return this.userService.update(+id, updateUserDto);
  }

  @Delete(':id')
  @ApiOperation({ summary: '删除用户' })
  @ApiResponse({ status: 200, description: '删除成功' })
  async remove(@Param('id') id: string) {
    return this.userService.remove(+id);
  }
}
```

#### 3.4.2 服务定义

```typescript
import { Injectable, NotFoundException, ConflictException } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { User } from './entities/user.entity';
import { CreateUserDto } from './dto/create-user.dto';
import { UpdateUserDto } from './dto/update-user.dto';

@Injectable()
export class UserService {
  constructor(
    @InjectRepository(User)
    private readonly userRepository: Repository<User>,
  ) {}

  async create(createUserDto: CreateUserDto): Promise<User> {
    const existingUser = await this.userRepository.findOne({
      where: { phone: createUserDto.phone },
    });

    if (existingUser) {
      throw new ConflictException('手机号已存在');
    }

    const user = this.userRepository.create(createUserDto);
    return await this.userRepository.save(user);
  }

  async findAll(query: any): Promise<{ list: User[]; total: number }> {
    const { page = 1, limit = 20, keyword } = query;
    const skip = (page - 1) * limit;

    const queryBuilder = this.userRepository.createQueryBuilder('user');

    if (keyword) {
      queryBuilder.andWhere('user.nickname LIKE :keyword', {
        keyword: `%${keyword}%`,
      });
    }

    const [list, total] = await queryBuilder
      .skip(skip)
      .take(limit)
      .getManyAndCount();

    return { list, total };
  }

  async findOne(id: number): Promise<User> {
    const user = await this.userRepository.findOne({
      where: { id },
    });

    if (!user) {
      throw new NotFoundException('用户不存在');
    }

    return user;
  }

  async update(id: number, updateUserDto: UpdateUserDto): Promise<User> {
    const user = await this.findOne(id);
    Object.assign(user, updateUserDto);
    return await this.userRepository.save(user);
  }

  async remove(id: number): Promise<void> {
    const user = await this.findOne(id);
    await this.userRepository.remove(user);
  }
}
```

#### 3.4.3 DTO定义

```typescript
import { IsString, IsEmail, IsOptional, IsEnum, MinLength, MaxLength } from 'class-validator';

export class CreateUserDto {
  @IsString()
  @MinLength(11)
  @MaxLength(11)
  phone: string;

  @IsString()
  @MinLength(8)
  password: string;

  @IsString()
  @IsOptional()
  nickname?: string;

  @IsString()
  @IsOptional()
  avatar?: string;

  @IsEnum(['male', 'female', 'other'])
  @IsOptional()
  gender?: string;
}

export class UpdateUserDto {
  @IsString()
  @IsOptional()
  nickname?: string;

  @IsString()
  @IsOptional()
  avatar?: string;

  @IsEnum(['male', 'female', 'other'])
  @IsOptional()
  gender?: string;
}
```

#### 3.4.4 实体定义

```typescript
import { Entity, Column, PrimaryGeneratedColumn, CreateDateColumn, UpdateDateColumn, DeleteDateColumn } from 'typeorm';

@Entity('users')
export class User {
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ unique: true })
  phone: string;

  @Column()
  password: string;

  @Column({ nullable: true })
  nickname: string;

  @Column({ nullable: true })
  avatar: string;

  @Column({ default: 'user' })
  role: string;

  @Column({ default: 1 })
  status: number;

  @CreateDateColumn()
  createdAt: Date;

  @UpdateDateColumn()
  updatedAt: Date;

  @DeleteDateColumn()
  deletedAt: Date;
}
```

### 3.5 异常处理

```typescript
import { ExceptionFilter, Catch, ArgumentsHost, HttpException, HttpStatus } from '@nestjs/common';
import { Request, Response } from 'express';

@Catch(HttpException)
export class HttpExceptionFilter implements ExceptionFilter {
  catch(exception: HttpException, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse<Response>();
    const request = ctx.getRequest<Request>();
    const status = exception.getStatus();
    const exceptionResponse = exception.getResponse();

    response.status(status).json({
      success: false,
      error: {
        code: exception.name,
        message: exception.message,
        details: exceptionResponse,
      },
      timestamp: new Date().toISOString(),
      path: request.url,
    });
  }
}
```

### 3.6 日志记录

```typescript
import { Injectable, NestInterceptor, ExecutionContext, CallHandler, Logger } from '@nestjs/common';
import { Observable } from 'rxjs';
import { tap } from 'rxjs/operators';

@Injectable()
export class LoggingInterceptor implements NestInterceptor {
  private readonly logger = new Logger(LoggingInterceptor.name);

  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {
    const request = context.switchToHttp().getRequest();
    const { method, url, body } = request;
    const now = Date.now();

    this.logger.log(`Request: ${method} ${url}`);

    return next.handle().pipe(
      tap(() => {
        const response = context.switchToHttp().getResponse();
        const { statusCode } = response;
        const delay = Date.now() - now;
        this.logger.log(`Response: ${method} ${url} ${statusCode} - ${delay}ms`);
      }),
    );
  }
}
```

---

## 4. 移动端开发规范

### 4.1 技术栈
- **框架**：React Native 0.73.x
- **语言**：TypeScript 5.x
- **导航**：React Navigation 6.x
- **状态管理**：Redux Toolkit
- **UI组件库**：React Native Elements / NativeBase
- **网络请求**：Axios
- **表单处理**：React Hook Form
- **样式方案**：Styled Components

### 4.2 项目结构

```
src/
├── assets/              # 静态资源
│   ├── images/         # 图片
│   ├── icons/          # 图标
│   └── fonts/          # 字体
├── components/         # 公共组件
│   ├── common/         # 通用组件
│   └── business/       # 业务组件
├── screens/            # 页面
│   ├── home/           # 首页
│   ├── community/      # 小区
│   ├── comment/        # 评论
│   └── user/           # 用户
├── navigation/         # 导航配置
├── hooks/              # 自定义Hooks
├── services/           # API服务
├── store/              # Redux状态管理
├── utils/              # 工具函数
├── types/              # TypeScript类型定义
├── constants/          # 常量定义
└── App.tsx             # 应用入口
```

### 4.3 命名规范

#### 4.3.1 文件命名
- **组件文件**：PascalCase，如 `UserProfile.tsx`
- **页面文件**：PascalCase，如 `HomeScreen.tsx`
- **工具文件**：camelCase，如 `formatDate.ts`
- **类型文件**：PascalCase，如 `UserTypes.ts`
- **常量文件**：PascalCase，如 `ApiConstants.ts`

#### 4.3.2 变量命名
- **变量**：camelCase，如 `userName`
- **常量**：UPPER_SNAKE_CASE，如 `MAX_FILE_SIZE`
- **组件**：PascalCase，如 `UserProfile`
- **函数**：camelCase，如 `getUserInfo`
- **接口**：PascalCase，I前缀，如 `IUser`
- **类型**：PascalCase，T前缀，如 `TUser`

### 4.4 代码规范

#### 4.4.1 组件定义

```typescript
import React from 'react';
import { View, Text, Image, TouchableOpacity, StyleSheet } from 'react-native';

interface UserProfileProps {
  userId: number;
  userName: string;
  avatar?: string;
  onUpdate?: (userId: number) => void;
}

export const UserProfile: React.FC<UserProfileProps> = ({
  userId,
  userName,
  avatar,
  onUpdate,
}) => {
  const handleUpdate = () => {
    onUpdate?.(userId);
  };

  return (
    <View style={styles.container}>
      <Image source={{ uri: avatar }} style={styles.avatar} />
      <Text style={styles.userName}>{userName}</Text>
      <TouchableOpacity style={styles.button} onPress={handleUpdate}>
        <Text style={styles.buttonText}>更新</Text>
      </TouchableOpacity>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 16,
    backgroundColor: '#ffffff',
    borderRadius: 8,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.1,
    shadowRadius: 8,
    elevation: 4,
  },
  avatar: {
    width: 48,
    height: 48,
    borderRadius: 24,
  },
  userName: {
    marginLeft: 12,
    fontSize: 16,
    fontWeight: '500',
    color: '#333333',
  },
  button: {
    marginLeft: 'auto',
    paddingHorizontal: 16,
    paddingVertical: 8,
    backgroundColor: '#1890ff',
    borderRadius: 4,
  },
  buttonText: {
    color: '#ffffff',
    fontSize: 14,
    fontWeight: '500',
  },
});
```

#### 4.4.2 自定义Hook

```typescript
import { useState, useEffect, useCallback } from 'react';
import { Alert } from 'react-native';

export const useUserInfo = (userId: number) => {
  const [userInfo, setUserInfo] = useState<IUser | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const fetchUserInfo = useCallback(async () => {
    setLoading(true);
    setError(null);
    try {
      const data = await UserService.getUserById(userId);
      setUserInfo(data);
    } catch (err) {
      setError(err.message);
      Alert.alert('错误', err.message);
    } finally {
      setLoading(false);
    }
  }, [userId]);

  useEffect(() => {
    fetchUserInfo();
  }, [fetchUserInfo]);

  return { userInfo, loading, error, refetch: fetchUserInfo };
};
```

#### 4.4.3 导航配置

```typescript
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import { HomeScreen } from '@/screens/home/HomeScreen';
import { CommunityDetailScreen } from '@/screens/community/CommunityDetailScreen';

const Stack = createNativeStackNavigator<RootStackParamList>();

export const AppNavigator: React.FC = () => {
  return (
    <Stack.Navigator>
      <Stack.Screen
        name="Home"
        component={HomeScreen}
        options={{ title: '首页' }}
      />
      <Stack.Screen
        name="CommunityDetail"
        component={CommunityDetailScreen}
        options={{ title: '小区详情' }}
      />
    </Stack.Navigator>
  );
};
```

---

## 5. 数据库开发规范

### 5.1 表命名规范
- **表名**：snake_case，如 `user_profiles`
- **字段名**：snake_case，如 `user_name`
- **索引名**：idx_表名_字段名，如 `idx_users_phone`
- **外键名**：fk_表名_字段名，如 `fk_comments_user_id`

### 5.2 字段规范
- **主键**：id，BIGINT类型，自增
- **时间字段**：created_at、updated_at、deleted_at，TIMESTAMP类型
- **状态字段**：status，TINYINT类型
- **软删除**：deleted_at，TIMESTAMP类型，NULL表示未删除
- **金额字段**：DECIMAL类型，保留2位小数
- **布尔字段**：TINYINT类型，0表示false，1表示true

### 5.3 索引规范
- **主键索引**：自动创建
- **唯一索引**：phone、email等唯一字段
- **普通索引**：查询频繁的字段
- **复合索引**：多字段联合查询
- **索引命名**：idx_表名_字段名

### 5.4 SQL规范

#### 5.4.1 表创建

```sql
CREATE TABLE users (
  id BIGSERIAL PRIMARY KEY,
  phone VARCHAR(20) UNIQUE NOT NULL,
  password VARCHAR(255) NOT NULL,
  nickname VARCHAR(50),
  avatar VARCHAR(255),
  gender TINYINT DEFAULT 0,
  birthday DATE,
  province VARCHAR(50),
  city VARCHAR(50),
  district VARCHAR(50),
  role VARCHAR(20) DEFAULT 'user',
  status TINYINT DEFAULT 1,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  deleted_at TIMESTAMP
);

CREATE INDEX idx_users_phone ON users(phone);
CREATE INDEX idx_users_city ON users(city);
CREATE INDEX idx_users_status ON users(status);
```

#### 5.4.2 查询语句

```sql
-- 使用索引字段
SELECT * FROM users WHERE phone = '13800138000';

-- 使用LIMIT限制结果
SELECT * FROM users WHERE city = '北京市' LIMIT 20;

-- 避免SELECT *
SELECT id, phone, nickname FROM users WHERE id = 1;

-- 使用JOIN优化
SELECT u.id, u.nickname, c.content
FROM users u
INNER JOIN comments c ON u.id = c.user_id
WHERE c.community_id = 1;
```

---

## 6. Git工作流规范

### 6.1 分支策略
- **main**：主分支，用于生产环境
- **develop**：开发分支，用于集成测试
- **feature/***：功能分支，用于新功能开发
- **bugfix/***：修复分支，用于bug修复
- **hotfix/***：紧急修复分支，用于生产环境紧急修复

### 6.2 分支命名
- **功能分支**：feature/功能描述，如 `feature/user-login`
- **修复分支**：bugfix/问题描述，如 `bugfix/login-error`
- **紧急修复**：hotfix/问题描述，如 `hotfix/security-fix`

### 6.3 提交规范
- **格式**：类型(范围): 描述
- **类型**：feat、fix、docs、style、refactor、test、chore
- **示例**：
  - feat(user): 添加用户登录功能
  - fix(comment): 修复评论点赞bug
  - docs(api): 更新API文档
  - style(ui): 优化用户界面样式
  - refactor(auth): 重构认证模块
  - test(user): 添加用户测试用例
  - chore(deps): 更新依赖包

### 6.4 合并规范
- **功能分支**：合并到develop分支
- **修复分支**：合并到develop分支
- **紧急修复**：合并到main和develop分支
- **合并方式**：使用Pull Request进行代码审查

---

## 7. 代码审查规范

### 7.1 审查要点
- **代码质量**：代码可读性、可维护性
- **功能完整性**：功能是否完整实现
- **性能优化**：是否存在性能问题
- **安全性**：是否存在安全隐患
- **测试覆盖**：测试用例是否完整
- **文档完整性**：文档是否完整

### 7.2 审查流程
1. 提交Pull Request
2. 指定审查人员
3. 审查人员审查代码
4. 提出修改意见
5. 开发人员修改代码
6. 审查通过后合并

### 7.3 审查标准
- **代码风格**：符合项目代码规范
- **命名规范**：符合命名规范
- **注释完整**：关键代码有注释
- **错误处理**：完善的错误处理
- **日志记录**：完整的日志记录
- **测试通过**：所有测试用例通过

---

## 8. 总结

### 8.1 规范要点
- **统一技术栈**：使用统一的技术栈
- **统一命名规范**：使用统一的命名规范
- **统一代码风格**：使用统一的代码风格
- **统一项目结构**：使用统一的项目结构
- **统一开发流程**：使用统一的开发流程

### 8.2 开发要点
- **代码质量**：保证代码质量
- **性能优化**：注重性能优化
- **安全加固**：注重安全加固
- **测试覆盖**：保证测试覆盖
- **文档完善**：完善文档

---

**文档结束**
